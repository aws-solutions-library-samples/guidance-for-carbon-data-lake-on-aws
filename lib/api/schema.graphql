schema {
    query: Query
    mutation: Mutation
}

type CalculatorOutput {
    record_id: String!
    asset_id: String
}

type PaginatedCalculatorOutput {
    items: [CalculatorOutput]
    nextToken: String
}

type Query {
    all(limit: Int, nextToken: String): PaginatedCalculatorOutput
    getOne(record_id: String!): CalculatorOutput
}

type Mutation {
    delete(record_id: String!): CalculatorOutput
}